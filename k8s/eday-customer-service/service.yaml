apiVersion: v1
kind: Service
metadata:
  name: eday-customer-service
  namespace: eday
spec:
  selector:
    app: eday-customer-service
  ports:
    - protocol: TCP
      port: 8083
      targetPort: 8083
  type: ClusterIP
