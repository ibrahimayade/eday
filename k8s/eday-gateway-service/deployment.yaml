

apiVersion: apps/v1
kind: Deployment
metadata:
  name: eday-gateway-service
  namespace: eday
spec:
  replicas: 2
  selector:
    matchLabels:
      app: eday-gateway-service
  template:
    metadata:
      labels:
        app: eday-gateway-service
    spec:
      containers:
        - name: gateway
          image: yadibrahim/eday-gateway-service:latest
          ports:
            - containerPort: 8881
          env:
            - name: DISCOVERY_SERVICE_URL
              value: "http://eday-discovery-service:8761/eureka"
            - name: CONFIG_SERVICE_URL
              value: "http://eday-config-service:9999"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8881
            initialDelaySeconds: 15
            periodSeconds: 10
